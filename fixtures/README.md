# Fixtures

These fixtures support the starter test matrix in [docs/implementation/starter-test-matrix.md](../docs/implementation/starter-test-matrix.md).

Expected JSON files are assertion snapshots for the first tests. They may intentionally omit fields that are validated elsewhere, such as exact source ranges, until the parser implementation stabilizes.

Fixture syntax follows the MVP subset in [syntax-examples.md](../docs/implementation/syntax-examples.md).

